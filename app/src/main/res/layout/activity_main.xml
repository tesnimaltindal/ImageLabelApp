<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".MainActivity">
    <!-- LinearLayout, içindeki bileşenleri sıralı bir şekilde (dikey veya yatay) yerleştirir.
Burada orientation="vertical" olduğu için öğeler üstten alta doğru yerleştirilir.
padding="16dp" tüm kenarlardan iç boşluk bırakır.   -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:id="@+id/objectImage"
        android:scaleType="centerCrop"
        android:layout_marginTop="16dp"
        android:src="@drawable/baseline_image_24"
        android:contentDescription="Captured Image"/>
    <!-- scaleType="centerCrop" → görseli ortalar ve kırparak tüm alanı doldurur.-->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/captureImgBtn"
        android:text="Capture Image"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/blue"
        android:layout_gravity="center"
        android:textSize="18sp"/>

    <!-- layout_gravity="center" → yatayda ortalanır (ama LinearLayout için tam genişlikte).-->

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/labelText"
        android:layout_gravity="center"
        android:layout_marginTop="80dp"
        android:textStyle="bold"
        android:textColor="@color/blue"
        android:textSize="50sp"

       />

</LinearLayout>